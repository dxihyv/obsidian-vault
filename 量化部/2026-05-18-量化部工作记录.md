# 2026-05-18 量化部工作记录

## 早报 Bug 修复 ×3

### 1. 昨日热门 +0.0%（盘前数据源问题）
- 开盘前热门板块数据全为0
- 修复方案：改用涨停板行业分布作为盘前热门板块展示
- 写入 skill：q1-morning-report-template Pitfall #2

### 2. sign_str 双负号修复（`--`）
- `sign_str()` 在 `abs()` 前被调用时产生 `--814(-5.8%)`
- 全局 grep 排查 `sign_str([^)]*)` 后跟非 `abs` 模式
- 修复方案：所有 sign_str 后必须紧跟 abs 调用
- 写入 skill：q1-morning-report-template + quant-sim-trading

### 3. 策略指标全0（旧版 SELL 记录缺 profit 字段）
- 旧版 SELL 记录无 `profit` 字段 → `h.get("profit", 0)` 读到全0
- 修复后策略指标从「胜率0%/均幅0%/回撤0%」变为正确值
- 写入 skill：quant-sim-trading Pitfall #25

## 模拟盘迭代

### 午休休眠机制
- 11:30-13:00 不推送任何消息
- 龙哥确认此行为
- 写入 skill：quant-sim-trading Pitfall #22

### 日报格式 3次迭代
1. 初始方案：格式压缩（删数据）→ 被龙哥否定
2. 最终方案：保留全量数据，加粗 + 分行分组 + `|` 对齐 + ¥前缀
3. 关键原则：**不删数据只改展示**
- 写入 skill：quant-sim-trading Pitfall #26

### 数值格式规范化
- 金额：`¥{:,.2f}` + 加粗
- 百分比：sign_str + abs 防双负
- 价格：统一 ¥ 前缀
- 防错口诀：`计算浮点 → 永远 {:.2f}`
- 写入 skill：scheduled-report-system

## 技能库更新

- `q1-morning-report-template` — Pitfall #2/#35/#37 更新，references 新增
- `quant-sim-trading` — Pitfall #22/#25/#26 新增/重写
- `scheduled-report-system` — 数值格式规范 + 跨文件审计流程
- 参考文件：references/2026-05-18-fixes.md（三合一修复记录）
