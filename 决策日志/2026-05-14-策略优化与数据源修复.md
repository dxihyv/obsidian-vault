# 2026-05-14 量化部工作记录

## 1. 早报选股策略优化

### RSI 推荐阈值收紧（morning_report.py）

**改动**：第938行
```python
# 旧
recommend = adjusted_score >= 50 and rsi14 <= 80 and macd_strong
# 新
recommend = adjusted_score >= 50 and rsi14 <= 70 and macd_strong
```

**原因**：RSI 70-80 区间原为"偏热"档（+0分但不拒绝），收紧到 ≤70 与策略C的 RSI<70 条件自洽，过滤掉偏热票。

### 新仓最低涨幅过滤（morning_report.py）

**改动**：第787-789行（is_pending=False 分支加涨幅门槛）
```python
if not is_pending and today_pct < 1.0:
    return None
```

**原因**：排除无量爬升的弱势票（q9量比扫描候选中涨幅0.3%-1%的票，缺乏动能支撑）。

### 剩余优化项（暂不动）

| 问题 | 结论 |
|------|------|
| 市值分段评分 | 改动大，边际收益不明 |
| 昨日涨停vs新信号分开排序 | 候选量不多，混排影响不大 |
| 主力资金数据加缓存 | 权重低，缓存收益有限 |
| 量比得分公式优化 | 可能导致分数虚高 |

## 2. 早盘买入价格修正（sim_trading.py）

### 全部改用实时价

**改动**：
- 删掉 `use_open` 时间分支判断（原9:25-9:40用开盘价）
- 删掉开盘价获取代码（curl + fields[5]）
- 统一用 `batch_realtime()` 获取实时价

**原因**：
- 开盘价字段不稳定（fields[5]有时是时间戳）
- 实时价已经能覆盖全天窗口（9:25-14:50）
- 逻辑简化，减少维护成本

**文件**：`/home/agentuser/quant/scripts/sim_trading.py`
- 第793行 docstring 更新
- 第33行用法注释更新
- 第810行删除 `use_open` 变量
- 第931-935行删除开盘价分支

## 3. 收盘报告数据源修复（generate_summary.py）

### 早报标的验证改读 today_candidates.csv

**问题**：原读取 `screening/选股结果_{DATE}.csv`（q9量比扫描的随机Top5），根本不是早报推荐的标的，验证了错误的数据。

**修复**：
- 改读 `outputs/today_candidates.csv`（早报实际推荐的Top2）
- 展示内容从"市值(亿)"改为"早报评分 + RSI"

**字段映射（today_candidates.csv）**：
| 字段 | 索引 |
|------|------|
| 代码 | cols[0] |
| 股票名称 | cols[1] |
| 综合评分（RSI加权后） | cols[5] |
| 今日涨幅 | cols[6] |
| 现价 | cols[8] |
| 止损价 | cols[9] |
| 目标1 | cols[10] |
| 买入区间低 | cols[13] |
| 买入区间高 | cols[14] |
| RSI | cols[15] |
| 筛选理由 | cols[17] |

## 4. 盯盘系统

- 收盘后盯盘进程正常退出（14:57）
- 亚星锚链触发真实持仓止损预警，已推送飞书

## 5. 待验证

- 明日早报验证 RSI≤70 + 涨幅≥1% 过滤效果
- 明日收盘报告验证标的验证板块是否正确读取 Top2
